{
  "name": "escape-velocity-chat",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "wrangler": "^4.67.0"
  },
  "scripts": {
    "dev": "wrangler dev --port ${API_PORT:-8787}",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.67.0"
  }
}
